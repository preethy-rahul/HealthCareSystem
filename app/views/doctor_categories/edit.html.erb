<h1>Editing </h1>
<%= form_for @doctorcategory do |f|%>
<%#= form_for :doctor_caregory, url: doctor_category_path(@doctorcategory), method: :patch do |f| %>
 
  <% if @doctorcategory.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@doctorcategory.errors.count, "error") %> prohibited
        this category from being saved:
      </h2>
      <ul>
        <% @doctorcategory.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :photo %><br>
    <%= f.file_field :photo %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', doctor_categories_path %>


